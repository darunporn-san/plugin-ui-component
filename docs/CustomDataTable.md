# 📊 CustomDataTable

สร้างตารางข้อมูลที่ปรับแต่งได้ พร้อมระบบแบ่งหน้าและเรียงลำดับ

## 🔧 วิธีใช้งานพื้นฐาน

```tsx
// กำหนดคอลัมน์
const columns = [
  {
    header: 'ชื่อ',
    accessorKey: 'name',
  },
  {
    header: 'อีเมล',
    accessorKey: 'email',
  },
  {
    header: 'สถานะ',
    accessorKey: 'status',
    cell: ({ row }) => (
      <Badge variant={row.original.status === 'active' ? 'success' : 'destructive'}>
        {row.original.status}
      </Badge>
    ),
  },
];

// ข้อมูล
const data = [
  { id: 1, name: 'สมชาย ใจดี', email: 'somchai@example.com', status: 'active' },
  { id: 2, name: 'สมหญิง กล้าหาญ', email: 'somying@example.com', status: 'inactive' },
];

// ใช้งาน
<CustomDataTable
  columns={columns}
  data={data}
  pageSize={10}
  onRowClick={(row) => console.log('Selected row:', row)}
/>
```

## 🎨 ตัวเลือกการแสดงผล

### ตำแหน่งของส่วนประกอบ
| ส่วนประกอบ | ตำแหน่งที่เป็นไปได้ |
|------------|-------------------|
| จำนวนข้อมูล | `topLeft`, `topRight`, `bottomLeft`, `bottomRight` |
| ตัวเลือกจำนวนแถวต่อหน้า | `topLeft`, `topRight`, `bottomLeft`, `bottomRight` |
| ตัวแบ่งหน้า | `top`, `bottom`, `both` |

### สไตล์
- `headerClassName` - คลาส CSS สำหรับหัวตาราง
- `rowClassName` - คลาส CSS สำหรับแถวข้อมูล
- `cellClassName` - คลาส CSS สำหรับเซลล์ข้อมูล
- `className` - คลาส CSS สำหรับตารางทั้งตาราง

## ⚙️ คุณสมบัติที่สำคัญ

### ข้อมูล
- `columns` (ต้องระบุ) - กำหนดคอลัมน์ของตาราง
- `data` (ต้องระบุ) - ข้อมูลที่จะแสดงในตาราง
- `pageSize` - จำนวนแถวต่อหน้า (ค่าเริ่มต้น: 10)

### การทำงาน
- `onRowClick` - ฟังก์ชันที่ทำงานเมื่อคลิกที่แถว
- `onPageChange` - ฟังก์ชันที่ทำงานเมื่อเปลี่ยนหน้า
- `onPageSizeChange` - ฟังก์ชันที่ทำงานเมื่อเปลี่ยนจำนวนแถวต่อหน้า
- `onSort` - ฟังก์ชันที่ทำงานเมื่อเรียงลำดับ

### การแบ่งหน้า
- `isPagination` - เปิด/ปิดการแบ่งหน้า (ค่าเริ่มต้น: true)
- `totalItems` - จำนวนข้อมูลทั้งหมด (ถ้าไม่ระบุจะใช้ความยาวของ data)
- `currentPage` - หน้าปัจจุบัน (สำหรับควบคุมจากภายนอก)

## 📱 ตัวอย่างการใช้งานจริง

### ตารางพร้อมการแบ่งหน้า
```tsx
<CustomDataTable
  columns={columns}
  data={users}
  pageSize={5}
  isPagination
  countPosition="bottomLeft"
  paginationPosition="bottomRight"
  pageSizePosition="topRight"
  onPageChange={(page) => console.log('Current page:', page)}
  onPageSizeChange={(size) => console.log('Page size:', size)}
/>
```

### ตารางพร้อมการเรียงลำดับ
```tsx
const sortableColumns = [
  {
    header: 'ชื่อ',
    accessorKey: 'name',
    sortable: true,
  },
  {
    header: 'วันที่สมัคร',
    accessorKey: 'createdAt',
    sortable: true,
    sortFn: 'datetime',
  },
  // ...
];

<CustomDataTable
  columns={sortableColumns}
  data={users}
  onSort={(sortBy, sortDirection) => {
    console.log('Sort by:', sortBy, 'Direction:', sortDirection);
  }}
/>
```

### ตารางพร้อมการเลือกแถว
```tsx
const [selectedRows, setSelectedRows] = useState([]);

const selectableColumns = [
  {
    id: 'select',
    header: ({ table }) => (
      <Checkbox
        checked={table.getIsAllPageRowsSelected()}
        onCheckedChange={(value) => table.toggleAllPageRowsSelected(!!value)}
      />
    ),
    cell: ({ row }) => (
      <Checkbox
        checked={row.getIsSelected()}
        onCheckedChange={(value) => row.toggleSelected(!!value)}
      />
    ),
  },
  // ...other columns
];

<CustomDataTable
  columns={selectableColumns}
  data={users}
  onRowSelectionChange={setSelectedRows}
/>
```

## 💡 เคล็ดลับ
- ใช้ `cell` renderer เพื่อปรับแต่งการแสดงผลของเซลล์
- ใช้ `header` renderer เพื่อปรับแต่งหัวตาราง
- ใช้ `footer` renderer เพื่อเพิ่มส่วนท้ายตาราง
- ใช้ `meta` เพื่อส่งข้อมูลเพิ่มเติมไปยัง cell renderer
- ใช้ `enableSorting` เพื่อเปิด/ปิดการเรียงลำดับทั้งตาราง
