# 🗂️ CustomDropdownMenu

สร้างเมนูแบบดรอปดาวน์ที่ปรับแต่งได้ ใช้งานง่าย และรองรับการทำงานที่หลากหลาย

## 🔧 วิธีใช้งานพื้นฐาน

```tsx
// ตัวอย่างการใช้งาน
<CustomDropdownMenu
  buttonText="ตัวเลือก"
  items={[
    { label: 'แก้ไข', onClick: () => console.log('แก้ไข') },
    { label: 'คัดลอก', onClick: () => console.log('คัดลอก') },
    { label: 'ลบ', onClick: () => console.log('ลบ'), variant: 'destructive' },
  ]}
/>
```

## 🎨 ตัวเลือกการแสดงผล

### รูปแบบปุ่ม (Variant)
| ค่า | ลักษณะ |
|------|---------|
| `default` | ปุ่มปกติ (ค่าเริ่มต้น) |
| `outline` | ปุ่มแบบมีกรอบ |
| `ghost` | ปุ่มแบบโปร่ง |
| `link` | ปุ่มแบบลิงก์ |

### ขนาด (Size)
| ค่า | ความสูง |
|------|---------|
| `sm` | 36px |
| `default` | 40px |
| `lg` | 44px |
| `icon` | 40x40px |

## ⚙️ คุณสมบัติที่สำคัญ

### ข้อมูลพื้นฐาน
- `buttonText` (ต้องระบุ) - ข้อความบนปุ่ม
- `items` (ต้องระบุ) - รายการเมนู `Array<{ label: string, onClick: () => void, disabled?: boolean, icon?: ReactNode, variant?: 'default' | 'destructive' }>`

### การทำงาน
- `onOpenChange` - ฟังก์ชันที่ทำงานเมื่อสถานะการเปิด/ปิดเมนูเปลี่ยน
- `disabled` - ปิดการใช้งานปุ่ม
- `align` - การจัดตำแหน่งเมนู (`start`, `center`, `end`)
- `side` - ตำแหน่งของเมนูที่ปรากฏ (`top`, `right`, `bottom`, `left`)

### การปรับแต่งสไตล์
- `buttonClassName` - คลาส CSS เพิ่มเติมสำหรับปุ่ม
- `contentClassName` - คลาส CSS เพิ่มเติมสำหรับเมนู
- `itemClassName` - คลาส CSS เพิ่มเติมสำหรับแต่ละรายการเมนู

## 📱 ตัวอย่างการใช้งานจริง

### เมนูจัดการผู้ใช้
```tsx
const handleEdit = (userId) => {
  console.log('แก้ไขผู้ใช้ ID:', userId);
};

const handleDelete = (userId) => {
  if (window.confirm('ยืนยันการลบผู้ใช้นี้?')) {
    console.log('ลบผู้ใช้ ID:', userId);
  }
};

<CustomDropdownMenu
  buttonText="จัดการ"
  items={[
    {
      label: 'ดูรายละเอียด',
      icon: <Eye className="mr-2 h-4 w-4" />,
      onClick: () => router.push(`/users/${userId}`)
    },
    {
      label: 'แก้ไข',
      icon: <Pencil className="mr-2 h-4 w-4" />,
      onClick: () => handleEdit(userId)
    },
    {
      label: 'ส่งรหัสผ่านใหม่',
      icon: <KeyRound className="mr-2 h-4 w-4" />,
      onClick: () => sendPasswordReset(userId)
    },
    {
      label: 'ระงับการใช้งาน',
      icon: <PauseCircle className="mr-2 h-4 w-4" />,
      onClick: () => suspendUser(userId)
    },
    {
      label: 'ลบ',
      icon: <Trash2 className="mr-2 h-4 w-4" />,
      variant: 'destructive',
      onClick: () => handleDelete(userId)
    }
  ]}
  buttonClassName="w-24"
  contentClassName="w-48"
  align="end"
/>
```

### เมนูแบบมีกลุ่ม
```tsx
<CustomDropdownMenu
  buttonText="ตัวเลือก"
  items={[
    {
      label: 'กลุ่มที่ 1',
      type: 'group',
      items: [
        { label: 'ตัวเลือก 1.1', onClick: () => console.log('1.1') },
        { label: 'ตัวเลือก 1.2', onClick: () => console.log('1.2') },
      ]
    },
    {
      label: 'กลุ่มที่ 2',
      type: 'group',
      items: [
        { label: 'ตัวเลือก 2.1', onClick: () => console.log('2.1') },
        { label: 'ตัวเลือก 2.2', onClick: () => console.log('2.2') },
      ]
    }
  ]}
  contentClassName="w-48"
/>
```

### เมนูแบบมีไอคอน
```tsx
import { 
  Download, 
  Copy, 
  Share2, 
  Star, 
  Trash2, 
  MoreHorizontal 
} from 'lucide-react';

<CustomDropdownMenu
  buttonText=""
  items={[
    {
      label: 'ดาวน์โหลด',
      icon: <Download className="mr-2 h-4 w-4" />,
      onClick: () => downloadFile(fileId)
    },
    {
      label: 'คัดลอกลิงก์',
      icon: <Copy className="mr-2 h-4 w-4" />,
      onClick: () => copyToClipboard(fileUrl)
    },
    {
      label: 'แชร์',
      icon: <Share2 className="mr-2 h-4 w-4" />,
      onClick: () => shareFile(fileId)
    },
    {
      label: 'เพิ่มในรายการโปรด',
      icon: <Star className="mr-2 h-4 w-4" />,
      onClick: () => addToFavorites(fileId)
    },
    {
      label: 'ลบ',
      icon: <Trash2 className="mr-2 h-4 w-4" />,
      variant: 'destructive',
      onClick: () => deleteFile(fileId)
    }
  ]}
  variant="ghost"
  size="icon"
  buttonClassName="h-8 w-8 p-0"
  contentClassName="w-48"
>
  <MoreHorizontal className="h-4 w-4" />
</CustomDropdownMenu>
```

## 💡 เคล็ดลับ
- ใช้ `icon` เพื่อเพิ่มไอค่อนในแต่ละรายการเมนู
- ใช้ `variant="destructive"` สำหรับการกระทำที่เสี่ยง เช่น การลบ
- ใช้ `disabled` เพื่อปิดการใช้งานรายการเมนู
- ใช้ `align` และ `side` เพื่อปรับตำแหน่งการแสดงผลเมนู
- ใช้ `contentClassName` เพื่อปรับแต่งสไตล์ของเมนู
- ใช้ `onOpenChange` เพื่อตรวจจับการเปิด/ปิดเมนู
