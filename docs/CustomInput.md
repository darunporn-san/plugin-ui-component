# 📝 CustomInput

สร้างฟิลด์ป้อนข้อมูลที่ปรับแต่งได้หลากหลาย พร้อมระบบตรวจสอบความถูกต้อง

## 🔧 วิธีใช้งานพื้นฐาน

```tsx
// ตัวอย่างการใช้งาน
<CustomInput
  name="username"
  label="ชื่อผู้ใช้"
  placeholder="กรุณากรอกชื่อผู้ใช้"
  rules={{ required: 'กรุณากรอกชื่อผู้ใช้' }}
/>
```

## 🎨 ตัวเลือกการแสดงผล

### ประเภทของ Input
| ประเภท | คำอธิบาย |
|--------|-----------|
| `text` | ข้อความปกติ (ค่าเริ่มต้น) |
| `email` | อีเมล |
| `password` | รหัสผ่าน (ซ่อนข้อความ) |
| `number` | ตัวเลข |
| `tel` | เบอร์โทรศัพท์ |
| `url` | URL |
| `textarea` | กล่องข้อความหลายบรรทัด |

### การจัดวาง Label
| ค่า | ลักษณะ |
|------|---------|
| `top` | Label อยู่ด้านบน (ค่าเริ่มต้น) |
| `left` | Label อยู่ด้านซ้าย |
| `right` | Label อยู่ด้านขวา |

## ⚙️ คุณสมบัติที่สำคัญ

### ข้อมูลพื้นฐาน
- `name` (ต้องระบุ) - ชื่อของฟิลด์
- `label` - ข้อความแสดงเหนือช่องกรอก
- `placeholder` - ข้อความตัวอย่างในช่องกรอก
- `type` - ประเภทของ input (ดูในหัวข้อประเภทของ Input)

### การตรวจสอบความถูกต้อง
- `rules` - กฎการตรวจสอบ (รองรับ react-hook-form rules)
- `required` - กำหนดให้ต้องกรอกข้อมูล
- `minLength` - ความยาวขั้นต่ำ
- `maxLength` - ความยาวสูงสุด
- `pattern` - รูปแบบที่ยอมรับ (RegExp)

### การทำงาน
- `onChange` - ฟังก์ชันที่ทำงานเมื่อค่ามีการเปลี่ยนแปลง
- `onBlur` - ฟังก์ชันที่ทำงานเมื่อออกจากช่องกรอก
- `disabled` - ปิดการใช้งานช่องกรอก
- `readOnly` - อ่านได้อย่างเดียว

## 📱 ตัวอย่างการใช้งานจริง

### ช่องกรอกข้อความพื้นฐาน
```tsx
<CustomInput
  name="fullname"
  label="ชื่อ-นามสกุล"
  placeholder="กรุณากรอกชื่อ-นามสกุล"
  required
/>
```

### ช่องกรอกรหัสผ่าน
```tsx
<CustomInput
  name="password"
  type="password"
  label="รหัสผ่าน"
  placeholder="กรุณากรอกรหัสผ่าน"
  rules={{
    required: 'กรุณากรอกรหัสผ่าน',
    minLength: {
      value: 8,
      message: 'รหัสผ่านต้องมีความยาวอย่างน้อย 8 ตัวอักษร'
    }
  }}
/>
```

### ช่องกรอกหลายบรรทัด
```tsx
<CustomInput
  name="address"
  label="ที่อยู่"
  type="textarea"
  rows={4}
  placeholder="กรุณากรอกที่อยู่"
  className="min-h-[120px]"
/>
```

### ช่องกรอกแบบมีหน่วย
```tsx
<div className="relative">
  <CustomInput
    name="weight"
    label="น้ำหนัก"
    type="number"
    placeholder="0.00"
    className="pr-10"
  />
  <span className="absolute right-3 bottom-2.5 text-muted-foreground">
    กก.
  </span>
</div>
```

## 💡 เคล็ดลับ
- ใช้ `rules` ร่วมกับ react-hook-form สำหรับการตรวจสอบความถูกต้อง
- สามารถใช้ `className` เพื่อปรับแต่งสไตล์เพิ่มเติมได้
- สำหรับฟิลด์ที่ต้องการการตรวจสอบพิเศษ ให้ใช้ `validate` ใน `rules`
- ใช้ `leftIcon` และ `rightIcon` เพื่อเพิ่มไอค่อนในช่องกรอกได้
