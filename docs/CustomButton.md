# 🎛️ CustomButton

สร้างปุ่มที่ปรับแต่งได้หลากหลาย พร้อมระบบยืนยันการกด

## 🔧 วิธีใช้งานพื้นฐาน

```tsx
// ตัวอย่างการใช้งาน
<CustomButton>คลิกที่ฉัน</CustomButton>
```

## 🎨 ตัวเลือกการแสดงผล

### สีสัน (Variant)

| ค่า | ลักษณะ |
|------|---------|
| `primary` | ปุ่มสีน้ำเงิน-ฟ้า (ค่าเริ่มต้น) |
| `secondary` | ปุ่มสีม่วง-ชมพู |
| `success` | ปุ่มสีเขียว |
| `warning` | ปุ่มสีส้ม |
| `danger` | ปุ่มสีแดง |
| `outline` | ปุ่มแบบกรอบ |
| `ghost` | ปุ่มแบบโปร่ง |
| `link` | ปุ่มแบบลิงก์ |

### ขนาด (Size)

| ค่า | ความสูง | ระยะห่างข้างใน |
|------|---------|---------------|
| `sm` | 36px | 12px |
| `default` | 40px | 16px |
| `lg` | 44px | 32px |
| `xl` | 56px | 40px |
| `icon` | 40x40px | - |

## ⚙️ คุณสมบัติที่สำคัญ

### เนื้อหา
- `children` (ต้องระบุ) - ข้อความหรือไอคอนในปุ่ม
- `leftIcon` - ไอคอนด้านซ้ายของข้อความ
- `rightIcon` - ไอคอนด้านขวาของข้อความ

### การทำงาน
- `onClick` - ฟังก์ชันที่ทำงานเมื่อคลิกปุ่ม
- `disabled` - ปิดการใช้งานปุ่ม
- `isLoading` - แสดงสถานะกำลังโหลด
- `fullWidth` - ยืดเต็มความกว้าง

### ตัวเลือกเพิ่มเติม
- `rounded` - ความโค้งมุม (`none`, `sm`, `md`, `lg`, `xl`, `full`)
- `shadow` - เงาของปุ่ม (`none`, `sm`, `default`, `md`, `lg`, `xl`, `inner`)
- `className` - เพิ่มคลาส CSS เพิ่มเติม

## 🛡️ ระบบยืนยันการกด

เพิ่มการยืนยันก่อนทำงานเมื่อคลิกปุ่ม:

```tsx
<CustomButton
  confirmTitle="ยืนยันการลบ"
  confirmMessage="คุณแน่ใจหรือไม่ว่าต้องการลบรายการนี้?"
  confirmActionText="ลบ"
  confirmCancelText="ยกเลิก"
  onConfirm={() => handleDelete()}
>
  ลบรายการ
</CustomButton>
```

## 📱 ตัวอย่างการใช้งานจริง

### ปุ่มปกติ
```tsx
<CustomButton>บันทึก</CustomButton>
```

### ปุ่มมีไอคอน
```tsx
<CustomButton leftIcon={<SaveIcon />}>
  บันทึก
</CustomButton>
```

### ปุ่มโหลด
```tsx
<CustomButton isLoading>
  กำลังประมวลผล...
</CustomButton>
```

### ปุ่มเต็มความกว้าง
```tsx
<CustomButton fullWidth>
  ดำเนินการต่อ
</CustomButton>
```

### ปุ่มลบแบบยืนยัน
```tsx
<CustomButton 
  variant="danger"
  confirmTitle="ยืนยันการลบ"
  confirmMessage="ไม่สามารถกู้คืนข้อมูลที่ลบได้"
  onConfirm={() => deleteItem(itemId)}
>
  ลบ
</CustomButton>
```
