# ☑️ CustomCheckbox

สร้างช่องทำเครื่องหมายที่ปรับแต่งได้หลากหลาย รองรับทั้งแบบตัวเลือกเดียวและหลายตัวเลือก

## 🔧 วิธีใช้งานพื้นฐาน

### แบบตัวเลือกเดียว
```tsx
<CustomCheckbox
  name="agree"
  label="ยอมรับเงื่อนไขและข้อตกลง"
  rules={{ required: 'กรุณายอมรับเงื่อนไข' }}
/>
```

### แบบหลายตัวเลือก
```tsx
const options = [
  { id: 'sports', label: 'กีฬา' },
  { id: 'music', label: 'ดนตรี' },
  { id: 'travel', label: 'ท่องเที่ยว' },
];

<CustomCheckbox
  name="interests"
  label="ความสนใจ"
  options={options}
  type="multiple"
  rules={{ required: 'กรุณาเลือกอย่างน้อย 1 ข้อ' }}
/>
```

## 🎨 ตัวเลือกการแสดงผล

### ประเภทของ Checkbox
| ประเภท | คำอธิบาย |
|--------|-----------|
| `single` | ตัวเลือกเดียว (ค่าเริ่มต้น) |
| `multiple` | หลายตัวเลือก |

### การจัดวาง
| ค่า | ลักษณะ |
|------|---------|
| `vertical` | เรียงแนวตั้ง (ค่าเริ่มต้น) |
| `horizontal` | เรียงแนวนอน |

## ⚙️ คุณสมบัติที่สำคัญ

### ข้อมูลพื้นฐาน
- `name` (ต้องระบุ) - ชื่อของฟิลด์
- `label` - ข้อความแสดงข้างช่องทำเครื่องหมาย
- `options` - ข้อมูลตัวเลือก (เมื่อใช้ type="multiple")
- `value` - ค่าปัจจุบัน (สำหรับ controlled component)

### การตรวจสอบความถูกต้อง
- `rules` - กฎการตรวจสอบ (รองรับ react-hook-form rules)
- `required` - กำหนดให้ต้องเลือก
- `validate` - ฟังก์ชันตรวจสอบแบบกำหนดเอง

### การทำงาน
- `onChange` - ฟังก์ชันที่ทำงานเมื่อค่ามีการเปลี่ยนแปลง
- `onBlur` - ฟังก์ชันที่ทำงานเมื่อออกจากช่องทำเครื่องหมาย
- `disabled` - ปิดการใช้งาน
- `readOnly` - อ่านได้อย่างเดียว

## 📱 ตัวอย่างการใช้งานจริง

### แบบมีเงื่อนไข
```tsx
const [isAgreed, setIsAgreed] = useState(false);

<>
  <CustomCheckbox
    name="terms"
    label="ยอมรับเงื่อนไขและข้อตกลง"
    onChange={(e) => setIsAgreed(e.target.checked)}
    required
  />
  
  <CustomButton 
    type="submit" 
    disabled={!isAgreed}
    className="mt-4"
  >
    ดำเนินการต่อ
  </CustomButton>
</>
```

### แบบหลายตัวเลือก
```tsx
const programmingLanguages = [
  { id: 'js', label: 'JavaScript' },
  { id: 'ts', label: 'TypeScript' },
  { id: 'py', label: 'Python' },
  { id: 'java', label: 'Java' },
];

<CustomCheckbox
  name="languages"
  label="ภาษาที่ถนัด"
  options={programmingLanguages}
  type="multiple"
  direction="horizontal"
  required
/>
```

### แบบมีไอคอน
```tsx
const notificationOptions = [
  { 
    id: 'email', 
    label: 'อีเมล',
    icon: <Mail className="mr-2 h-4 w-4" />
  },
  { 
    id: 'sms', 
    label: 'ข้อความ',
    icon: <MessageSquare className="mr-2 h-4 w-4" />
  },
  { 
    id: 'push', 
    label: 'แจ้งเตือน',
    icon: <Bell className="mr-2 h-4 w-4" />
  },
];

<CustomCheckbox
  name="notifications"
  label="การแจ้งเตือน"
  options={notificationOptions}
  type="multiple"
  className="space-y-2"
  optionClassName="flex items-center"
/>
```

## 💡 เคล็ดลับ
- ใช้ `type="multiple"` สำหรับการเลือกได้หลายค่า
- ใช้ `direction="horizontal"` สำหรับการแสดงตัวเลือกแนวนอน
- สามารถใช้ `className` และ `optionClassName` เพื่อปรับแต่งสไตล์เพิ่มเติม
- ใช้ `renderOption` เพื่อปรับแต่งการแสดงผลของแต่ละตัวเลือก
