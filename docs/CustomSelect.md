# 🔘 CustomSelect

สร้าง Dropdown เลือกข้อมูลที่ปรับแต่งได้หลากหลาย พร้อมระบบค้นหาและกรองข้อมูล

## 🔧 วิธีใช้งานพื้นฐาน

```tsx
// ตัวอย่างการใช้งาน
const options = [
  { value: 'th', label: 'ไทย' },
  { value: 'en', label: 'English' },
  { value: 'ja', label: '日本語' },
];

<CustomSelect
  name="language"
  label="ภาษา"
  options={options}
  placeholder="เลือกภาษา"
  rules={{ required: 'กรุณาเลือกภาษา' }}
/>
```

## 🎨 ตัวเลือกการแสดงผล

### การจัดวาง Label
| ค่า | ลักษณะ |
|------|---------|
| `top` | Label อยู่ด้านบน (ค่าเริ่มต้น) |
| `left` | Label อยู่ด้านซ้าย |
| `right` | Label อยู่ด้านขวา |

### ตัวเลือกเพิ่มเติม
- `isMulti` - เลือกได้หลายค่า
- `isSearchable` - เปิดใช้งานการค้นหา
- `isClearable` - แสดงปุ่มล้างค่า
- `isDisabled` - ปิดการใช้งาน

## ⚙️ คุณสมบัติที่สำคัญ

### ข้อมูลพื้นฐาน
- `name` (ต้องระบุ) - ชื่อของฟิลด์
- `label` - ข้อความแสดงเหนือช่องเลือก
- `options` - ข้อมูลตัวเลือก (Array ของ {value, label})
- `placeholder` - ข้อความแสดงเมื่อยังไม่ได้เลือกค่า
- `defaultValue` - ค่าเริ่มต้น

### การตรวจสอบความถูกต้อง
- `rules` - กฎการตรวจสอบ (รองรับ react-hook-form rules)
- `required` - กำหนดให้ต้องเลือกข้อมูล

### การทำงาน
- `onChange` - ฟังก์ชันที่ทำงานเมื่อค่ามีการเปลี่ยนแปลง
- `onBlur` - ฟังก์ชันที่ทำงานเมื่อออกจากช่องเลือก
- `isLoading` - แสดงสถานะกำลังโหลด
- `isClearable` - แสดงปุ่มล้างค่า

## 📱 ตัวอย่างการใช้งานจริง

### เลือกประเทศ
```tsx
const countries = [
  { value: 'th', label: 'ประเทศไทย' },
  { value: 'us', label: 'สหรัฐอเมริกา' },
  { value: 'jp', label: 'ญี่ปุ่น' },
  { value: 'kr', label: 'เกาหลีใต้' },
];

<CustomSelect
  name="country"
  label="ประเทศ"
  options={countries}
  placeholder="เลือกประเทศ"
  isSearchable
  required
/>
```

### เลือกหลายค่า
```tsx
const interests = [
  { value: 'sports', label: 'กีฬา' },
  { value: 'music', label: 'ดนตรี' },
  { value: 'travel', label: 'ท่องเที่ยว' },
  { value: 'food', label: 'อาหาร' },
];

<CustomSelect
  name="interests"
  label="ความสนใจ"
  options={interests}
  placeholder="เลือกความสนใจ"
  isMulti
  closeMenuOnSelect={false}
/>
```

### แบบโหลดข้อมูลจากภายนอก
```tsx
const [isLoading, setIsLoading] = useState(false);
const [options, setOptions] = useState([]);

const loadOptions = async (inputValue) => {
  setIsLoading(true);
  const response = await fetch(`/api/search?q=${inputValue}`);
  const data = await response.json();
  setOptions(data);
  setIsLoading(false);
};

<CustomSelect
  name="user"
  label="ค้นหาผู้ใช้"
  options={options}
  onInputChange={loadOptions}
  isLoading={isLoading}
  isSearchable
  placeholder="พิมพ์เพื่อค้นหา..."
/>
```

## 💡 เคล็ดลับ
- ใช้ `isSearchable` เมื่อมีตัวเลือกจำนวนมาก
- ใช้ `isMulti` สำหรับการเลือกได้หลายค่า
- ใช้ `isLoading` เมื่อโหลดข้อมูลจาก API
- สามารถใช้ `formatOptionLabel` เพื่อปรับแต่งการแสดงผลของแต่ละตัวเลือก
- ใช้ `components` เพื่อปรับแต่งส่วนประกอบต่างๆ ของ Select
