# 📅 CustomCalendar

สร้างปฏิทินสำหรับเลือกวันที่หรือช่วงวันที่ที่ปรับแต่งได้

## 🔧 วิธีใช้งานพื้นฐาน

### เลือกวันที่เดียว
```tsx
<CustomCalendar
  mode="single"
  name="birthdate"
  label="วันเกิด"
  placeholder="เลือกวันเกิด"
  rules={{ required: 'กรุณาเลือกวันเกิด' }}
/>
```

### เลือกช่วงวันที่
```tsx
<CustomCalendar
  mode="range"
  name="dateRange"
  label="ช่วงวันที่"
  placeholder="เลือกช่วงวันที่"
  rules={{ required: 'กรุณาเลือกช่วงวันที่' }}
/>
```

## 🎨 ตัวเลือกการแสดงผล

### โหมดการเลือก
| โหมด | คำอธิบาย |
|------|-----------|
| `single` | เลือกวันที่เดียว (ค่าเริ่มต้น) |
| `range` | เลือกช่วงวันที่ |
| `multiple` | เลือกได้หลายวัน |

### การจัดรูปแบบวันที่
| รูปแบบ | ตัวอย่าง |
|--------|---------|
| `dd/MM/yyyy` | 31/12/2023 |
| `yyyy-MM-dd` | 2023-12-31 |
| `PPP` | December 31st, 2023 |
| `PP` | Dec 31, 2023 |

## ⚙️ คุณสมบัติที่สำคัญ

### ข้อมูลพื้นฐาน
- `name` (ต้องระบุ) - ชื่อของฟิลด์
- `label` - ข้อความแสดงเหนือช่องเลือกวันที่
- `placeholder` - ข้อความแสดงเมื่อยังไม่ได้เลือกวันที่
- `format` - รูปแบบการแสดงผลวันที่ (ค่าเริ่มต้น: dd/MM/yyyy)

### ข้อจำกัด
- `minDate` - วันที่เริ่มต้นที่เลือกได้
- `maxDate` - วันที่สิ้นสุดที่เลือกได้
- `disabledDates` - วันที่ที่ไม่สามารถเลือกได้
- `disabledDays` - วันในสัปดาห์ที่เลือกไม่ได้ (0=อาทิตย์, 1=จันทร์, ...)

### การตรวจสอบความถูกต้อง
- `rules` - กฎการตรวจสอบ (รองรับ react-hook-form rules)
- `required` - กำหนดให้ต้องเลือกวันที่
- `validate` - ฟังก์ชันตรวจสอบแบบกำหนดเอง

## 📱 ตัวอย่างการใช้งานจริง

### เลือกวันเกิด
```tsx
<CustomCalendar
  name="birthdate"
  label="วันเกิด"
  placeholder="เลือกวันเกิด"
  maxDate={new Date()}
  required
  fromYear={1900}
  toYear={new Date().getFullYear()}
  captionLayout="dropdown"
/>
```

### เลือกช่วงวันจองห้องพัก
```tsx
const [dateRange, setDateRange] = useState({
  from: undefined,
  to: undefined,
});

// ฟังก์ชันตรวจสอบช่วงวันที่
const validateDateRange = (value) => {
  if (!value?.from || !value?.to) {
    return 'กรุณาเลือกช่วงวันที่ให้ครบถ้วน';
  }
  
  const diffTime = Math.abs(value.to - value.from);
  const diffDays = Math.ceil(diffTime / (1000 * 60 * 60 * 24));
  
  if (diffDays > 14) {
    return 'สามารถจองได้ไม่เกิน 14 วัน';
  }
  
  return true;
};

<CustomCalendar
  mode="range"
  name="bookingDate"
  label="ช่วงวันที่เข้าพัก"
  placeholder="เลือกช่วงวันที่เข้าพัก"
  value={dateRange}
  onChange={setDateRange}
  minDate={new Date()}
  disabledDates={[new Date(2023, 11, 25), new Date(2023, 11, 31)]}
  disabledDays={[0, 6]} // ปิดวันเสาร์-อาทิตย์
  rules={{ validate: validateDateRange }}
  numberOfMonths={2}
  className="rounded-md border"
/>
```

### ปฏิทินแบบมีเวลา
```tsx
<CustomCalendar
  name="appointmentTime"
  label="นัดหมายเวลา"
  placeholder="เลือกวันและเวลานัดหมาย"
  showTimeSelect
  timeFormat="HH:mm"
  timeIntervals={30}
  timeCaption="เวลา"
  dateFormat="dd/MM/yyyy HH:mm"
  minTime={new Date(0, 0, 0, 9, 0)} // 09:00
  maxTime={new Date(0, 0, 0, 18, 0)} // 18:00
  filterTime={(time) => {
    // จำกัดเวลาเฉพาะช่วงเวลาทำการ
    const hour = time.getHours();
    return hour >= 9 && hour < 18;
  }}
  required
/>
```

## 💡 เคล็ดลับ
- ใช้ `mode="range"` สำหรับการเลือกช่วงวันที่
- ใช้ `showTimeSelect` เพื่อเพิ่มการเลือกเวลา
- ใช้ `minDate` และ `maxDate` เพื่อจำกัดช่วงวันที่ที่เลือกได้
- ใช้ `disabledDates` และ `disabledDays` เพื่อปิดการเลือกวันที่เฉพาะ
- ใช้ `filterDate` และ `filterTime` สำหรับการกรองวันที่และเวลาที่ซับซ้อน
